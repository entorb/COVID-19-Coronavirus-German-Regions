name: Test

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the master branch
on:  
  workflow_dispatch:   
  push: 
#    branches: [ master ]
    branches-ignore:
      - '**'


# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  job_1:
    runs-on: ubuntu-20.04
    steps:  
#    - name: test locale
#      run: |
#        locale -a
#        # sudo apt-get install -y locales
#        # locale -a
#        cd /usr/share/locales
#        sudo ./install-language-pack de_DE.UTF-8
#        locale -a

    - name: test
      run: |
        mkdir -p data2
        cd data2
        touch test.txt
        tar cfz  ../cache/test.tgz *

    - name: Upload Release Asset
      id: upload-release-asset 
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        # upload_url: ${{ steps.create_release.outputs.upload_url }} # This pulls from the CREATE RELEASE step above, referencing it's ID to get its outputs object, which include a `upload_url`. See this blog post for more info: https://jasonet.co/posts/new-features-of-github-actions/#passing-data-to-future-steps 
        # upload_url: https://github.com/octocat/Hello-World/releases/download/v1.0.0/example.zip
        upload_url: https://github.com/entorb/COVID-19-Coronavirus-German-Regions/releases/download/tip/plots-gnuplot.tgz
        asset_path: cache/test.tgz
        asset_name: test.
        asset_content_type: application/gzip
